package main

import (
	"fmt"
	// "os"
)


func main() {
	fmt.Println("TupleAssignment")
	// A case which uses error
	// f, err = os.Open("test")

	// cases which use ok; but not always return two value
	// map lookup
	// v, ok = m[T]
	// type assertion
	// v, ok = x.(T)
	// channel receive
	// v, ok = <-ch

	// use "_" to drop unusing value
}